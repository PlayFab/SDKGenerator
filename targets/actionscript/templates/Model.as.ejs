
package com.playfab.<%- api.name %>Models
{
<% if(needsPlayFabUtil) { %>    import com.playfab.PlayFabUtil;

<% } %><%- getDeprecationComment("    ", datatype)
%>    public class <%- datatype.name %><%- getBaseTypeSyntax(datatype) %>
    {
<% for(var i in datatype.properties) { var property = datatype.properties[i] %><%- getDeprecationComment("        ", property)
%>        public var <%- getModelPropertyDef(property, datatype) %>;
<% } %>
        public function <%- datatype.name %>(data:Object=null)
        {
            if(data == null)
                return;
<% for(var i in datatype.properties) { var property = datatype.properties[i]
%><%- getModelPropertyInit("            ", property, datatype) %>
<% } %>
        }
    }
}
