
<% if(datatype.description) { %>
	/// <summary>
	/// <%- datatype.description %>
	/// </summary><% } %>
@interface <%- datatype.name %> : NSObject
		<% for(var i in datatype.properties) { var property = datatype.properties[i] %>
		<% if(property.description) { %>
		/// <summary>
		/// <%- property.description %>
		/// </summary><% } %><%- getPropertyAttribs(property, datatype) %>
		@property <%- getPropertyDef(property, datatype) %> <% } %>;<% if(isResultHandler(datatype)){ var className = datatype.name.split('Result')[0]; %>
		@property id* Request;
		@property id* CustomData;
		<%}%>
@end

@implementation <%- datatype.name %>

@end