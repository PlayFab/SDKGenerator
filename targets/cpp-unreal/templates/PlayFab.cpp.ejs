////////////////////////////////////////////////////////////////////////////////////////////// 
// Automatically generated cpp file for the UE4 PlayFab plugin.
// This cpp file is the main cpp file for the plugin. We set up globals here.
// Edit the variables between the "Edit Here Start" and "Edit Here Stop" Comments
//
// API Version: <%- apiRevision %>
// SDK Version: <%- sdkVersion %>
//////////////////////////////////////////////////////////////////////////////////////////////

#include "PlayFabPrivatePCH.h"

///////////////////////// 
// Edit Here Start
////////////////////////

// Replace each <> pair with the correct information from your PlayFab Dashboard

const FString	IPlayFab::PlayFabURL(TEXT("https://<GameTitleID>.playfabapi.com"));
const FString	IPlayFab::PlayFabLogicURL(TEXT("https://<GameTitleID>.playfablogic.com"));
const FString	IPlayFab::PlayFabAppId(TEXT("<GameTitleID>"));
const int32		IPlayFab::CloudScriptVersionInt(1);
const FString	IPlayFab::PlayFabApiKey(TEXT("<PlayFabAPISecretKey>"));
const FString	IPlayFab::PhotonRealtimeAppId(TEXT("<PhotonRealtimeAppID>"));
const FString	IPlayFab::PhotonTurnbasedAppId(TEXT("<PhotonTurnbasedAppID>"));
const FString	IPlayFab::PhotonChatAppId(TEXT("<PhotonChatAppID>"));

////////////////////////
// Edit Here Stop
////////////////////////

class FPlayFab : public IPlayFab
{
	/** IModuleInterface implementation */
	virtual void StartupModule() override
	{
		//Force classes to be compiled on shipping build
		UPlayFabJsonObject::StaticClass();
		UPlayFabJsonValue::StaticClass();
		<%
		// This Loops through the different apis and adds the classes here 
		for(var i in names)
		{%>
		UPlayFab<%- names[i].name %>API::StaticClass();<%
		}%>
	}

	virtual void ShutdownModule() override
	{

	}
};

IMPLEMENT_MODULE( FPlayFab, PlayFab )

DEFINE_LOG_CATEGORY(LogPlayFab);