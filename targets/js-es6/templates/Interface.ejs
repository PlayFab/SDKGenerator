<%- generateApiSummary("", datatype, "summary", "https://api.playfab.com/Documentation/" + api.name + "/datatype/" + datatype.classNameSpace + "/" + datatype.classNameSpace + "." + datatype.name)
%>export interface I<%- datatype.name %><%- getBaseTypeSyntax(datatype) %> {
<% if (datatype.properties) { for (var i = 0; i < datatype.properties.length; i++) { var property = datatype.properties[i]
%><%- generateApiSummary("    ", property, "description")
%>    <%- property.name %><%- getPropertyTsType(property, datatype) %>;
<% } } %>}

